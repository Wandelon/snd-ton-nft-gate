{
  "name": "snd-ton-contracts",
  "private": true,
  "scripts": {
    "postinstall": "node scripts/patch-blueprint.cjs",
    "build": "blueprint build",
    "test": "blueprint test",
    "deploy:testnet": "blueprint run deploy_testnet --testnet --mnemonic"
  },
  "dependencies": {
    "@ton/core": "^0.63.0",
    "@ton/crypto": "^3.3.0",
    "@ton/blueprint": "^0.33.0",
    "dotenv": "^16.6.1"
  }
}

